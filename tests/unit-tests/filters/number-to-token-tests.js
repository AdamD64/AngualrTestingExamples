/*
(function() {
    describe('number to token filter tests', function() {
        var filter;
        beforeEach(module('myApp.filters'));

        beforeEach(inject(function(numberToToken){
            filter = numberToToken;
        }));

        it('TODO', function(){
            //expect.....filter(..val..) to....
        });
    });
}());

*/
